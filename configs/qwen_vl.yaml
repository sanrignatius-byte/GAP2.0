# Qwen3-VL-30B-A3B-Thinking (MoE) specific configuration
# Inherits from default.yaml

model:
  name: "/projects/_hdd/myyyx1/data-process-test/Qwen3-VL-30B-A3B-Thinking"
  num_layers: 48
  hidden_dim: 2048
  num_visual_tokens: null  # Dynamic resolution in Qwen3-VL
  device: "auto"  # 30B MoE 模型需要多 GPU，auto 自动分配
  dtype: "bfloat16"

analysis:
  probe_targets: ["visual_token", "text_instruction_token", "answer_token"]
  expected_cliff_range: [20, 36]
  probe_layers: [0, 4, 8, 12, 16, 20, 24, 28, 32, 36, 40, 44, 47]
